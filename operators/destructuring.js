let arr = [10, 20, 30];

let [a, b] = arr;

console.log(a); // 10
console.log(b); // 20

// Destructuring allows us to unpack values from arrays or properties from objects into distinct variables.
// In this example, we are destructuring the first two elements of the arr array into variables a and b.
/**
 * map → modify

filter → select

reduce → combine

some → any?

every → all?

spread → expand

rest → collect
 */