let nums = [1, 2, 3];

let doubled = nums.map(n => n * 2);

console.log(doubled); // [2, 4, 6]
//map function is used to create a new array by applying a function to each element of the original array. 
//In this example, we are doubling each number in the nums array and creating a new array called doubled.
