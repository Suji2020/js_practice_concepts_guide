function greet(name = "Guest") {
  console.log(`Hello, ${name}`);
}

greet();        // Hello, Guest
greet("Suji");  // Hello, Suji
//let change | const fixed | arrow = short | template = ${} | module = import/export | ?. safe | ?? fallback | default param = backup